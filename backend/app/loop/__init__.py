"""Kun Mode-A autonomous loop (W3 ownership).

planner -> patcher -> runner -> evaluator -> decider, plus the deterministic
closed constraint loop. All events are emitted through ``kun_log`` only.
"""
